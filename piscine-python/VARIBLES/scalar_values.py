# Define the following variables:
#   - an integer called `epochs`
#   - a float called `learning_rate`
#   - a string called `model_name`
#   - a boolean `use_gpu`
# print each variable with its choice

epochs = 7
learning_rate = 0.1
model_name = "sonnet"
use_gpu = True
print(f"epochs: {epochs}")
print(f"learning_rate: {learning_rate}")
print(f"model_name: {model_name}")
print(f"use_gpu: {use_gpu}")